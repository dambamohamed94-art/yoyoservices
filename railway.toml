[build]
builder = "NIXPACKS"
buildCommand = "cd apps/api && pnpm install && npx prisma generate && npx prisma migrate deploy && pnpm build"

[deploy]
startCommand = "cd apps/api && node dist/src/main.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10